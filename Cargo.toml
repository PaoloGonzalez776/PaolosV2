[package]
name = "cerebro_build"
version = "0.1.0"
edition = "2024"

[dependencies]
chromiumoxide = { version = "0.9.1", features = ["fetcher", "zip8"], default-features = false }
futures = "0.3.32"
scraper = "0.25.0"
tokio = { version = "1.50.0", features = ["full"] }
